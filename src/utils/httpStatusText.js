// Define constants for response status messages to ensure consistent terminology
const SUCCESS = "success";
const FAIL = "fail";
const ERROR = "error";

module.exports={
    SUCCESS,
    FAIL,
    ERROR
}